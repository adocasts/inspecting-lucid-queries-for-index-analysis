@layout({ title: 'Jumpstart' })

  <h1 class="font-bold text-2xl mt-8">
    Posts
  </h1>
  
  <p class="text-gray-600 mb-6">
    Found: {{ posts.total.toLocaleString() }}
  </p>

  <ul>
    @each (post in posts)
      <li>{{ post.title }}</li>
    @endeach
  </ul>

@end